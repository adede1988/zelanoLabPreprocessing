## Running preprocessing

## Task 1
- Using the dataTracking excel sheet, identify data files that are available on the server but that have not yet been preprocessed



## Task 2
- Organize participant folders on the server to match the organization of other participants who have already had their data extracted. Do not delete any files. 
- Place the raw data into a folder called AtlasData
- For EEG_breathing parcticipants, write load data scripts for all participants with raw data that has not yet been extracted. Examine the load data matlab scripts that have been written in the subject folders of other subjects in the same Type in order to understand the structure of the load data script. 
- Note: for 260806_EEG_NWU_JH and 260806_EEG_NWU_MM there are two experimental tasks that have been combined into a single recording
- To slice the two sessions with only one recording, look for the portion of the data that does versus does not have event markers in the event channel. Since there is a load data file per session, you can hard code the split point for each session after determining it in scratch code. 
- To identify event markers, you can identify the square wave pulses in the event signal. The last approximately 30 minutes of data will have event markers, the earlier part of the recording will not. Make the split 2 minutes before the onset of the period with square wave pulses. 
- Run all of your newly written load data scripts to extract all of this new data and create raw data 

## Task 3
- For the breathingTasks sessions, all data that have been extracted will need to be preprocessed whether they have been in the past or not
- Any that lack necessary parameters, put guess parameters into the dataTracking sheet to use, make sure they are specified as guess parameters
- Switch the breath detection and parameterization engine to the breathMetrics toolbox functions available in github/breathMetrics. Compare performance for 3 test sessions between breathMetrics based segmentation and segmentation based on the old functions. You may need to prepare the data with some smoothing, mean centering, or standardized scaling. You will know if preprocessing is needed because you will see large disagreement between breathMetrics and the old engine. Agreement should be very high
- add all available breath characteristic quantifications that breathMetrics provides to the behDat output in the preprocessing
- run preprocessing for all breathingTasks sessions that have curated parameters
- start a running list of guess parameter sessions for me to go through manually later

## Task 4
- Run preprocessing for the O15 task for any subjects for whom data have been extracted but not yet preprocessed. 
- Add to your running list of guess parameter sessions, add guess parameters to the excel sheet, don't run preprocessing for these sessions as I will need to do it manually to check parameters

## Task 5
- Run preprocessing for the threshold task for any subjects for whom data have been extracted but not yet preprocessed. 
- Add to your running list of guess parameter sessions, add guess parameters to the excel sheet, don't run preprocessing for these sessions as I will need to do it manually to check parameters

## Task 6
- Run preprocessing for the odor cue task  for any subjects for whom data have been extracted but not yet preprocessed. 
- For the odor cue task, some recent subjects have been run with a no-cue condition. This no-cue condition is coded with cue == 0 in the raw behavioral data. Examine the cue task pre processing to see how the raw data are imported, read some of the most recent cue task raw behavioral data files, find one where there are 0 cue trials, figure out how to adapt the cue task pre processing scripts and functions so that no cue trials can be handled while remaining backward compatible for older files
- Add to your running list of guess parameter sessions, add guess parameters to the excel sheet, don't run preprocessing for these sessions as I will need to do it manually to check parameters

## Task 7
- look in the github/ZelanoLabScripts folder for old files handling preprocessing of the emotionalMovieTask
- Create a new set of preprocessing code files for handling the emotionalMovieTask in the style of the breathingTasks preprocessing, including the new update to breathMetrics based breath segmentation and quantification
- Note: one photodiode pulse means neutral, two means happy, and three means sad in coding for the videos that people were watching. 
- The onset of a new video clip is always coded with one two or three photodiode pulses, this logic should already be available in the old preprocessing scripts
- Add ECG handling similar to breathingTasks if that is not already part of handling the emotionalMovieTask and also making sure that data cleaning and respiratory processing is being used by up to date scripts. 
- Add guess stop points and outputs for assessment into the preprocessing scripts and functions like in the breathingTasks
- rerun preprocessing for any sessions with curated emotionalMovieTask data parameters
- For any without curated parameters, add guesses if there are no guesses yet and add to your running list for me to do manually later
- For EEGbreathing participants, go ahead and run the preprocessing even if the parameters are guesses, but make sure to evaluate that a reasonable number of breaths and heartbeats are being detected. There's no macro data for these recordings, so all macro and spike related parameters are irrelevant. Leave these participants marked as guess parameters even though you'll have run them. I will go back and check the outputs before switching the parameters to curated

## Task 8
- There is a new task called alternating6Blocks
- This task is structurally similar to breathingTasks but with some key differences
- Raw behavioral data files with participant responses to emotion and awareness rating questions are contained in my google drive folder cZelano/breathingDataFiles. Files are named with PARTICIPANTID_mindfulBreathing_DATETIME.csv. 
- In addition, there are sniffLogicLog_DATETIME.csv files that contain behavioral computer recordings of the respiration signal. These behavioral computer recordings include time, airflow, and event columns. The event columns indicate active trial time 0.4, active trial time 0.0, and audiobook listening periods. The active trial time 0.4 periods can be coded as "focused_entrainment". The active trial time 0.0 periods can be coded as "focused_noEntrain". The audiobook periods can be coded as "audiobook". 
- To figure out time alignment between the sniffLogicLog and the mindfulBreathing files, it is enough to identify how each block of affective and awareness rating corresponds to the blocks of the task. There is an initial pre-task baseline set of question ratings. Then there is a set of question ratings given after the audiobook block and after each of the focused_noEntrain and focused_entrainment blocks. In this way, you can figure out the alignment. 
- To align the sniffLogicLog with the .mat raw data, you can utilize the airflow data available in the respiration signals in both recordings. in these data files CSC31 in the original channel labels is always the good breathing signal in the raw .mat data. Write code that establishes the temporal alignment between the sniffLocicLog and raw .mat data. Then establish start and end times for each of the conditions: audiobook, focused_noEntrain, and focused_entrainment
- Write preprocessing code that will construct behDat tables in the outDat .mat files and processed behavior .csv files for output that are formated the same way as the breathingTasks outputs. Only include breaths in these outputs that are during one of the conditions. Between block intervals can be ignored
- Because these files were all recorded using standardized methods, you can guess the 


## Task 9
- We've started to move to collecting breathingTasks without the affective/awareness ratings and without a single file
- Instead, there are recordings that are separate for audioBook, focusedBreathing, sleep, sleepWithOdor, restingBaseline, and distractedBreathing.
- distractedBreathing should be recoded as audiobook in the output data in behDat 
- Can you write a new breathingTasks_separate pipeline that will import the multiple files for different conditions and integrate into a single output file with the same format as breathingTasks
- Since there are no affective or awareness ratings, these columns will be left off in the behDat and processed behavior outputs
- you'll have to guess parameters for these since they won't have curated parameters yet
- There's no need to worry about segmentation of the data into condition blocks since one file equals on condition. 
- The data in the final output should be concatenated together across all available files within a session
- make a "TTL" field in the same format as the breathingTasks but the TTL pulse times will just indicate the start and end of concatenated sections. 
- breathMetrics should be used in the same way as for breathingTasks to do the breath segmentation and quantification
- These will likely be all guess parameter sessions, so make guesses and add them to the list for me to look at


