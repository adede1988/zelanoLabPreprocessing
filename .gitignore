# machine-specific path override (never commit)
labPaths_local.m

# session tracking sheet is lab data, not code — supply it locally, do not commit
dataTracking*.xlsx

# data / outputs
*.mat
*.asv
*.mex*
processedBehavior/
figures/
*.png
*.jpg
*.jpeg
# ...except the breath-segmentation QC overlays, which are review deliverables
!breathingQualityCheck/*.jpg
!breathingQualityCheck/**/*.jpg
# (the guess-review report is no longer kept in the repo: batch/qc8_guessReviewFigs.m +
#  batch/buildGuessReview.ps1 build it on the lab machine; 2026-09-25 edition is on R:
#  under Adam\Dupi_processing\guessReview_260925\)

# OS cruft
.DS_Store
Thumbs.db
